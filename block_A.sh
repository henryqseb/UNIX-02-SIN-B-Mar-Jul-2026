gpg --version //It is used 
to display the current version of GnuPG
which gpg //Display the path 
of the executable file in the system
gpg --full-generate-key //Genearte the
new keys
gpg --list-keys //List all our keys
gpg --list-secret-keys --keyid-format=long // Display a list of all 
my private keys and make them long
